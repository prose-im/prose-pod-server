# HG changeset patch
# User Rémi Bardon <remi@remibardon.name>
# Date 1758354269 -25200
#      Sat Sep 20 14:44:29 2025 +0700
# Node ID c37333c4fe8619939711fb4663522c70359c4944
# Parent  7a67c5be766c51527ee9fcb2c51ad29996e8561b
mod_groups_shell: Fix return message of `groups:sync_group`

diff -r 7a67c5be766c -r c37333c4fe86 mod_groups_shell/mod_groups_shell.lua
--- a/mod_groups_shell/mod_groups_shell.lua	Sat Sep 20 02:29:19 2025 +0700
+++ b/mod_groups_shell/mod_groups_shell.lua	Sat Sep 20 14:44:29 2025 +0700
@@ -56,12 +56,10 @@
 
 	-- Perform group subscriptions (e.g. if it was delayed when adding a member).
 	-- NOTE: This operation is O(n^2).
-	ok, err = mod_groups.sync(group_id)
-	if ok then
-		return true, "Synchronised group " .. group_id
-	else
-		return ok, err
-	end
+	mod_groups.sync(group_id);
+
+	-- NOTE: `mod_groups.sync` cannot fail.
+	return true, "Synchronised group " .. group_id
 end
 
 function shell_env.groups:add_member(host, group_id, username, delay_update)
